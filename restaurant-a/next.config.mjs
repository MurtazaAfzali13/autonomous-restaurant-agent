/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // proxy: {
    //   "/admin/:path*": {
    //     target: "/admin-protected/:path*",
    //     rewrite: true,
    //   },
    // },
  },
};

export default nextConfig;